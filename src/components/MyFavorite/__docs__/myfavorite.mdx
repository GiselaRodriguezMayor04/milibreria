import { Canvas, Meta } from "@storybook/blocks";
import Example from "./example.tsx";
import * as MyFavoriteStories from "./myfavorite.stories.tsx";

<Meta of={MyFavoriteStories} title="MyFavorite" />

# MyFavorite

Botón de ícono personalizado basado en Material-UI con el ícono de favorito.

#### Example - Primary

<Canvas of={MyFavoriteStories.Primary} />

## Usage

```tsx
import { MyFavorite } from 'milibreria';

function Example() {
  return (
    <MyFavorite
      iconColor="red"
      disabled={false}
      onClick={() => alert("Icon clicked")}
    />
  );
}

export default Example;